<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miko.story">

    <application
        android:name=".base.StoryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Story">
        <activity
            android:name=".presentation.story.AddStoryActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.story.StoryDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.membership.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.story.StoryActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.membership.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>